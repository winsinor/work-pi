# TODO

No open items. All previously tracked rendering/performance work has been
completed (RGB565 fast path, shared conversion helper, numpy acceleration,
stats font caching, custom image caching, layout caching, custom-images
glob caching, stale-data indicator). The weather alert banner feature was
removed entirely rather than finished — see git history if it's ever
wanted back.
